version: '3.7'

services:
  flask:
    environment:
      - PORT=8081
    build: .
    ports:
      - 8081:8081
    depends_on:
      - memcached
  memcached:
    image: memcached:latest
    ports:
      - 11211:11211